# DESIGN.md — TicketEscape Design System「CONSOLE（管制盤）」

TicketEscape の UI デザインシステム。`escape.id` の発売時刻に **一発勝負** でカートを確保する時限ツール、という本質に合わせて、画面を *SaaS ダッシュボード* ではなく **管制盤（コンソール）** として設計する。

このドキュメントは「色の使い方」を中心に、原則・トークン・コンポーネント・状態設計・2画面のレイアウト・アクセシビリティまでを規定する**唯一の正典**。実装（`options/` `popup/` の HTML/CSS 書き換え）はこの仕様に従う。

- 既存 UI からの脱却ポイント: インディゴ紫グラデ（`#6c63ff`）、絵文字アイコン（🎫⚙️🎟📊）、ラジアルグラデ背景 ＝ いわゆる「AI 量産 SaaS テンプレ」の指紋。**これらを全廃**する。
- 正準モード: **ダーク**。ライトは副系として併記。

---

## 目次

1. [設計原則](#1-設計原則)
2. [カラーシステム（中核）](#2-カラーシステム中核)
3. [状態 → 色のマッピング（状態機械）](#3-状態--色のマッピング状態機械)
4. [タイポグラフィ](#4-タイポグラフィ)
5. [余白・グリッド・形状・エレベーション](#5-余白グリッド形状エレベーション)
6. [モーション](#6-モーション)
7. [アイコンとブランド](#7-アイコンとブランド)
8. [コンポーネント仕様](#8-コンポーネント仕様)
9. [画面レイアウト](#9-画面レイアウト)
10. [アクセシビリティ](#10-アクセシビリティ)
11. [ライティング（マイクロコピー）](#11-ライティングマイクロコピー)
12. [実装方針とトークン配布](#12-実装方針とトークン配布)
13. [旧 → 新 移行マップ](#13-旧--新-移行マップ)

---

## 1. 設計原則

7 つの原則。迷ったら必ずここに戻る。

1. **計器であって、アプリではない。** すべての要素は機能で存在を正当化する。装飾のための装飾を置かない。
2. **色はテレメトリ（状態信号）。** 色はムードでなく「今どの作戦フェーズか」を一瞬で伝える計器の信号。配色はフェーズ駆動の状態機械（§3）として設計する。
3. **カウントダウンが主役。** 画面で最も重要な数字を、最も大きく・最も明るく・等幅（tabular）で据える。視線が必ずそこへ落ちる階層を作る。
4. **静 → 動。** 平常時は静かでほぼ無彩。モーションと輝度は T-0 が近づくにつれてのみ現れる。抑制こそが信号を際立たせる。
5. **一目で「今どの状態か」。** 常設のステータス系（チップ＋色＋ラベル）が「フェーズ」を即答する。
6. **失敗しない導線。** 5 ステップの手順を常に可視化し、次の操作が常に最も明るいボタンになる。破壊的・不可逆な操作だけが赤。
7. **脱・絵文字。** 絵文字は使わない。単線（モノライン）アイコン＋本物のワードマークに置き換える。これ 1 つで「AI テンプレ感」の最大の指紋が消える。

---

## 2. カラーシステム（中核）

### 2.1 3 層モデル

色を 3 つの役割だけに厳格に分ける。**この線引きを破らないことがシステムの命。**

| 層 | 役割 | 使用比率の目安 | 色 |
|----|------|----------------|----|
| **Neutral（無彩）** | 筐体・面・文字。UI の地。 | 約 90〜95% | 寒色寄りのチャコール群 |
| **Signal（シグナル）** | ブランド＝**唯一の差し色**。能動・武装・主要アクション。 | 約 5% | アンバー `#FFB224` のみ |
| **Semantic（意味固定）** | 結果の通知のみ。 | 数 % | 緑＝成功 / 赤＝失敗・不可逆 |

> **黄金律:** アンバーが至る所にあると「武装」を意味しなくなる。**希少性＝信号性。** 1 画面のアンバー焦点は原則 1 つ（＝いま押すべきボタン、または進行中のカウントダウン。発射時は両者が一致する）。

### 2.2 ダークパレット（正準）

```css
:root {
  color-scheme: dark;

  /* ── Chassis / 無彩ベース ── */
  --te-bg:          #0B0E13;                 /* 盤面（ページ背景） */
  --te-surface:     #11161E;                 /* 計器面（カード/パネル） */
  --te-surface-2:   #171D27;                 /* インセット（入力・テーブル見出し） */
  --te-line:        rgba(255,255,255,0.09);  /* ヘアライン境界 */
  --te-line-strong: rgba(255,255,255,0.16);  /* 強境界 / hover */

  /* ── Text ── */
  --te-text:        #E7ECF2;  /* 主要 */
  --te-text-2:      #AEB8C7;  /* 副次 */
  --te-text-mut:    #8C97A8;  /* 補助・ヒント */
  --te-text-dim:    #5C6675;  /* 不活性 */

  /* ── Signal（amber / ブランド・武装色）── */
  --te-signal:      #FFB224;  /* インク用（文字・線・数字・アイコン） */
  --te-signal-fill: #FFB224;  /* 面用（実体ボタン・ドット・スウォッチ） */
  --te-signal-up:   #FFC247;  /* hover / T-MINUS 強調（輝度up） */
  --te-signal-dn:   #C8861A;  /* pressed / 低強調 */
  --te-signal-tint: rgba(255,178,36,0.12);   /* チップ/ウェルの地 */
  --te-signal-line: rgba(255,178,36,0.38);   /* チップ枠・フォーカス環 */
  --te-on-signal:   #1A1206;  /* amber 面上の文字（純黒でなく濃茶で温度を保つ） */

  /* ── Semantic（意味固定）── */
  --te-ok:          #3FB950;
  --te-ok-tint:     rgba(63,185,80,0.13);
  --te-on-ok:       #08220E;
  --te-err:         #F8513D;
  --te-err-tint:    rgba(248,81,61,0.13);
  --te-on-err:      #2A0A06;

  /* ── Radius（§5）── */
  --te-r-sm:   6px;    /* チップ・小入力 */
  --te-r-md:   10px;   /* 入力・ボタン */
  --te-r-lg:   14px;   /* カード・パネル */
  --te-r-pill: 999px;  /* ステータスピル・番号 */

  /* ── Elevation（影は最小限）── */
  --te-shadow-menu: 0 8px 24px rgba(0,0,0,0.5);  /* メニュー類のみ */

  /* ── Motion（§6）── */
  --te-dur-fast: 120ms;
  --te-dur-base: 180ms;
  --te-ease:     cubic-bezier(0.2,0.8,0.2,1);
}
```

> radius / elevation / motion は色と同じ正準 `:root` に同居させる（§5・§6 はその解説）。モード非依存なのでライト副系では上書き不要（影のみ後述で差し替え）。

**意図的に「青」を持たない。** 装飾的なブルー/パープルは AI テンプレの典型なので排除し、情報・補助はすべて無彩で表現する。これがパレットを締め、脱 AI を決定づける。

### 2.3 ライトパレット（副系）

**ダークは固定の正準モード**（プロダクト方針＝「ダーク中心」）。OS の `prefers-color-scheme` には**追従しない** — ライトはあくまで将来の**手動切替**用に `<html data-theme="light">` で opt-in する副系として定義する（OS がライトでも UI はダークのまま）。要点は **シグナルの「インク」と「面」を分離**すること。

```css
:root[data-theme="light"] {
    color-scheme: light;

    --te-bg:          #EEF1F6;
    --te-surface:     #FFFFFF;
    --te-surface-2:   #F4F6FB;
    --te-line:        rgba(16,22,34,0.10);
    --te-line-strong: rgba(16,22,34,0.18);

    --te-text:        #121722;
    --te-text-2:      #3B4660;
    --te-text-mut:    #6B768C;
    --te-text-dim:    #9AA3B4;

    --te-signal:      #B26A06;  /* インクは白地で AA を満たす濃いめの琥珀へ */
    --te-signal-fill: #FFB224;  /* 面（ボタン地）は鮮やかなまま据え置き */
    --te-signal-up:   #9A5C05;
    --te-signal-dn:   #C8861A;
    --te-signal-tint: rgba(178,106,6,0.10);
    --te-signal-line: rgba(178,106,6,0.40);
    --te-on-signal:   #1A1206;

    --te-ok:          #1E8E3E;
    --te-ok-tint:     rgba(30,142,62,0.12);
    --te-on-ok:       #08220E;
    --te-err:         #D93025;
    --te-err-tint:    rgba(217,48,37,0.12);
    --te-on-err:      #2A0A06;

    --te-shadow-menu: 0 8px 24px rgba(16,22,34,0.16);
}
```

> **「インク」と「面」の使い分け（重要）**
> - 大きな数字（カウントダウン）・本文中のアンバー文字・アイコン・境界線 = `--te-signal`（テーマで変化。ダーク #FFB224 / ライト #B26A06）。
> - 実体の塗りボタン・ドット・スウォッチ = `--te-signal-fill`（両モード #FFB224）＋ 文字は `--te-on-signal`。
> これを守らないとライトモードで琥珀文字が白地に溶けて読めなくなる。

### 2.4 コントラスト（WCAG）

ダーク基盤での実測コントラスト比（背景 `--te-bg #0B0E13`）。すべて本文 AA（4.5:1）以上。

| 前景 | 比 | 用途 |
|------|----|------|
| `--te-text #E7ECF2` | ≈ 16:1 | 本文 |
| `--te-text-mut #8C97A8` | ≈ 6.5:1 | 補助文字 |
| `--te-signal #FFB224` | ≈ 10.7:1 | カウントダウン・差し色文字 |
| `--te-ok #3FB950` | ≈ 7.5:1 | 成功 |
| `--te-err #F8513D` | ≈ 5.7:1 | 失敗 |
| `--te-on-signal #1A1206` on `#FFB224` | ≈ 10.4:1 | ボタン上文字 |

### 2.5 使用ルール（Do / Don't）

- **Do** — アンバーは「いま重要な 1 つ」だけ。主要アクション or 進行中カウントダウン。
- **Do** — 赤は「失敗」と「不可逆操作（行削除など）」のみ。
- **Do** — 緑は「成功・取得完了」など肯定的確定のみ。
- **Do** — 色は必ずラベル/アイコン/形と併用し、色だけに意味を載せない（色覚多様性対応）。
- **Don't** — グラデーション（ブランド面・背景とも）。指紋になるので原則禁止。塗りはフラット。
- **Don't** — 「注意・次にこれ」を赤で出す。それはアンバーの淡い喚起（attention 状態 §8）で表現する。
- **Don't** — 装飾目的の着色テキスト。本文は常に無彩。

---

## 3. 状態 → 色のマッピング（状態機械）

このツールの「色の使い方」の心臓部。作戦フェーズが色・カウントダウン・ステータスチップ・主要アクション・モーションを駆動する。

| フェーズ | 日本語ラベル | 主色 | カウントダウン | ステータスチップ | 主要アクション | モーション |
|----------|--------------|------|----------------|------------------|----------------|------------|
| `IDLE` | 設定中 | 無彩 | `--:--:--`（`--te-text-dim`） | 無彩ドット「待機なし」 | **実行待機**（amber 実体） | なし |
| `ARMED` | 実行待機中 | `--te-signal` | 残時間（amber, 秒進行） | amber「STANDBY」＋ドット点滅 | 設定変更時のみ表示 | ドット 1.4s 点滅 |
| `T-MINUS` | まもなく（≤60s 目安） | `--te-signal-up` | amber 増輝・微拡大 | amber「T-MINUS」 | — | 点滅高速化＋scale 1.0→1.03 |
| `FIRING` | 実行中 | `--te-signal-up` | `00:00:0x` amber 最大強度 | amber「FIRING」 | — | 連続パルス（最速） |
| `SUCCESS` | 確保成功 | `--te-ok` | `00:00:00 · 成功` | 緑「SECURED」 | 設定を開く/再設定 | なし（確定の静止） |
| `FAILED` | 失敗 | `--te-err` | `失敗` | 赤「FAILED」 | 再設定 | なし |
| `EXPIRED` | 時刻超過・要再設定 | 無彩＋赤縁 | `00:00:00`（dim） | 赤縁「再設定が必要」 | 再設定（amber） | なし |

**強度で緊迫を出す（色相は動かさない）:** T-MINUS / FIRING はアンバーのまま、輝度（`--te-signal` → `--te-signal-up`）・点滅速度・微小スケールだけで切迫を伝える。赤への色シフトはしない＝「赤＝失敗/不可逆」の意味を汚さないため。

**Attention（次にこれ、の喚起）:** エラーではない誘導はアンバーの淡いパルスで対象コントロールを示す。既存の 2 挙動を踏襲・再設計する。
- URL を変更して未「確認」 → **確認**ボタンが amber パルス。
- 数量が全て 0 のまま → **実行待機**ボタンが amber パルス＋ツールチップ「数量を設定してください」。

---

## 4. タイポグラフィ

### 4.1 書体（2 系統）

- **UI / 本文**: システムフォントスタック（Web フォント不使用 = 高速・ネイティブ・脱 Inter/Geist の AI 感）。
  `--te-font: -apple-system, BlinkMacSystemFont, "Segoe UI", system-ui, "Hiragino Sans", "Yu Gothic UI", sans-serif;`
- **データ / 数字 / 技術値（必須）**: 等幅。タイマー・残時間・URL・ms・並列数・ログなど **数値と機械的な値はすべて mono**。これが計器感の核。
  `--te-mono: "SF Mono", ui-monospace, SFMono-Regular, "JetBrains Mono", Menlo, Consolas, monospace;`
  - 数字には常に `font-variant-numeric: tabular-nums;` を付け、桁を揃える（カウントダウンが暴れない）。
  - 任意のアップグレード: バンドル可能なら `Space Mono` / `IBM Plex Mono` を mono 先頭に追加すると計器の個性が一段強まる（拡張なので外部 CDN 非依存・同梱が前提）。

### 4.2 タイプスケール

| ロール | サイズ / ウェイト / 字間 | 用途 |
|--------|--------------------------|------|
| Countdown hero | 44–56px / 500 / +0.04em / **mono** | メインの残時間 |
| Countdown sub | 24–28px / 500 / mono | ポップアップの残時間 |
| h1 | 22px / 700 / -0.01em | ページ見出し |
| Section title | 16px / 700 | パネル見出し |
| Eyebrow / status label | 11px / 600 / +0.12em / **UPPERCASE** / mono | 計器ラベル（例: `STANDBY` `READOUT`） |
| Body | 14px / 400 / line-height 1.6 | 本文 |
| Help / caption | 12px / 400 / `--te-text-mut` | 補足 |
| Data value | 13–14px / 500 / mono / tabular | 設定値・数量 |

- **ウェイトは 400 / 500 / 600 / 700 の 4 段のみ。**
- **ケース:** 英字の Eyebrow とステータスチップのみ UPPERCASE mono（計器ジャンルの約束事＝テレメトリのラベルに見せる、意図的な例外）。それ以外の英文・ボタン・文章はセンテンスケース。日本語ラベルは通常表記。

---

## 5. 余白・グリッド・形状・エレベーション

- **基準単位 4px。** スケール: `4 · 8 · 12 · 16 · 20 · 24 · 32 · 48`。縦リズムは 16/24/32 を多用。
- **角丸（機械的に、やや締める）:**
  - `--te-r-sm: 6px;`（チップ・小入力）
  - `--te-r-md: 10px;`（入力・ボタン）
  - `--te-r-lg: 14px;`（カード・パネル）
  - `--te-r-pill: 999px;`（ステータスピル・ステップ番号）
  - 既存の 20px は緩すぎる → 14px に締めて精密感を出す。
- **境界線:** ヘアライン（1px、`--te-line`）でパネルを仕切る。**重い影に依存しない。**
- **エレベーション:** ダーク計器 UI では影はほぼ見えない。**面の明度差（`bg < surface < surface-2`）＋境界線**で階層を表す。ドロップシャドウはメニュー類のみ最小限（`--te-shadow-menu: 0 8px 24px rgba(0,0,0,.5)`）。AI 的な大きく柔らかい紫の影は使わない。
- **レイアウト幅:** options 本文 `min(900px, 100%)`。popup `360px`。

---

## 6. モーション

**原則は「静 → 動」。平常時はモーションを置かない。動きは必ず状態を伝える。**

- 許可するモーション:
  - ステータスドット点滅（STANDBY/FIRING）: opacity/scale、1.4s ease-in-out（FIRING は ~0.7s に高速化）。
  - カウントダウン: 通常は数値更新のみ。T-MINUS で hero が微拡大（1.0→1.03）しアンバーが増輝、点滅が速まる。
  - ボタン hover: `translateY(-1px)` ＋わずかな地色変化。active: `translateY(0)` / `scale(.98)`。
  - フォーカス環: `0 0 0 3px var(--te-signal-line)`（唯一許可する「発光的」表現＝機能的フォーカス）。
  - Attention 喚起: 対象コントロールの淡い amber パルス（§3）。
- トークン:
  - `--te-dur-fast: 120ms; --te-dur-base: 180ms;`
  - 点滅/パルス: `1.0–1.4s`。
  - イージング: 入場 `cubic-bezier(0.2,0.8,0.2,1)`、パルス `ease-in-out`。
- **`prefers-reduced-motion: reduce` を尊重:** 点滅・拡大を停止し、色の状態表現だけ残す。

---

## 7. アイコンとブランド

### 7.1 絵文字の全廃 → 単線アイコン

`🎫⚙️🎟📊🔗⏱🎪` をすべて廃止し、`currentColor` で着色できる単線（outline）アイコンに置換。

- 方針: **SVG シンボルスプライトを同梱**（`icons/icons.svg`、`<use>` 参照）。外部 CDN 非依存でオフライン動作・テーマ追従。
- 必要アイコン（最小セット）: `clock` / `target`（武装）/ `link` / `sliders`（詳細設定）/ `ticket` / `list` / `check` / `x` / `plus` / `trash` / `alert` / `refresh`。
- スタイル: ストローク 1.5px・線端 round・サイズ 16–20px（装飾 24px 上限）。塗りつぶしアイコンは使わない。
- 出典の参考: Tabler / Lucide / Phosphor の outline をベースに同梱（ライセンス確認のうえ）。

### 7.2 ロゴ / ワードマーク

グラデ角丸＋🎫 を廃止。**レティクル（照準環）＋ T-0 ティック**を中核にした 1 色マークにする。意味は「狙った瞬間を正確に撃つ」。

- マーク: 開いた照準環＋上端（12 時方向）に T-0 を示すアンバーのティック。`currentColor`（環）＋ `--te-signal`（ティック）の 2 色。
- ワードマーク: `TicketEscape` を字間を締めた sans、または mono。"E" まわり or 末尾ティックにアンバーの差し色。
- 出発点の SVG（実装はこれを磨く）:

```html
<svg viewBox="0 0 32 32" width="28" height="28" fill="none" aria-hidden="true">
  <circle cx="16" cy="16" r="11" stroke="currentColor" stroke-width="1.5" stroke-dasharray="2 3.4"/>
  <circle cx="16" cy="16" r="3" stroke="currentColor" stroke-width="1.5"/>
  <line x1="16" y1="1.5" x2="16" y2="7"  stroke="#FFB224" stroke-width="2" stroke-linecap="round"/>
</svg>
```

---

## 8. コンポーネント仕様

各コンポーネントを「構造（anatomy）＋状態」で定義。括弧内は現行コードの対応要素。

### 8.1 ステータスチップ（`.s-badge`）
ピル。先頭ドット＋ mono UPPERCASE ラベル。バリアント: `idle / armed / t-minus / firing / success / failed / expired`。色は §3 に従い、`armed/firing` のドットのみ点滅。文字色 = 各色の `--te-*`（インク）、地 = 対応 tint。

### 8.2 カウントダウン表示（hero）
構造: eyebrow ラベル（`カウントダウン` 等）＋巨大 mono tabular 数値＋サブメッセージ（`で遷移します` 等）。色はフェーズ追従。**画面で最も目立つ単一要素。**

### 8.3 パネル / カード（`.card`）
計器面。ヘッダ = アイコン＋タイトル＋（任意でステータス）。本体はヘアラインで区切る。地 `--te-surface`、境界 `--te-line`、角丸 `--te-r-lg`。

### 8.4 フィールド / フォーム行（`.field`）
ラベル（広幅時は左）＋入力＋ヘルプ＋インライン検証。エラー時はヘルプを `--te-err`、枠を赤に。

### 8.5 入力（text / number / datetime）
インセットのウェル: 地 `--te-surface-2`、ヘアライン、フォーカスで amber 環。数値・URL・日時の **値は mono**。

### 8.6 ボタン
| 種別 | 地 | 文字 | 用途 |
|------|----|------|------|
| primary（go） | `--te-signal-fill` 実体 | `--te-on-signal` | いま押すべき主アクション（実行待機）。画面で最も明るい唯一 |
| confirm | 透明＋ amber 枠 | `--te-signal` | 確認（券種取得） |
| secondary | 透明＋ `--te-line-strong` 枠 | `--te-text-2` | 行追加など。hover で amber tint |
| danger / remove | 透明 | `--te-text-mut` → hover `--te-err` | 行削除など不可逆 |

状態: hover/active/focus（§6）、disabled（`--te-text-dim`・地 `--te-surface-2`）、attention（amber パルス・§3）。

### 8.7 ステップレール（5 ステップ導線）
番号ノード＋短いラベルの水平レール。完了/現在ステップが amber に点灯し、進捗が一目で分かる（既存 `.guide-steps` を「光るレール」へ再設計）。「失敗しない導線」原則の体現。

### 8.8 券種数量テーブル / 行
ダークの境界付きリスト行。`券種名` 入力＋ **数量ステッパー（− 値 +）** ＋削除。素の number 入力でなく ±ボタン付きステッパーにしてタップ目標を大きく、当日操作の誤りを減らす。

### 8.9 ステータス / ログ読み取り（`.status-display`）
端末風の mono 読み取り。`HH:MM:SS` タイムスタンプ＋行。「READOUT」eyebrow。

### 8.10 トグル / チェックボックス
カスタム。ON で amber。`accent-color: var(--te-signal-fill)` を最低限の実装に。

### 8.11 空状態（popup）
計器の「無信号」表現。`target` アイコン（dim）＋「設定がありません」＋導線ボタン。

### 8.12 ツールチップ
ダーク・ヘアライン・mono ラベル可。

---

## 9. 画面レイアウト

### 9.1 Options（管制コンソール）

- **ヘッダ（sticky）:** 左にワードマーク、右に常設ステータスチップ。ARMED 時は小型ライブカウントダウンも表示。
- 推奨は **2 ペインのコックピット**（広幅時）:
  - 左ペイン = 設定フォーム（SETUP）。
  - 右ペイン = **ミッションステータス（sticky）**: 巨大カウントダウン hero ＋ステータスチップ＋券種サマリ。常に視界に残す。
  - 狭幅では 1 カラムに積む。
- パネル構成:
  1. `SETUP / 作戦設定`: ステップレール → URL（確認後はイベント名読み取りを併記）→ 実行時刻 → 詳細設定（折りたたみ）。アクション `[確認] [実行待機]`。
  2. `TICKETS / 券種`: 数量ステッパー付きテーブル。
  3. `READOUT / ログ`: 端末風ステータス/ログ。

### 9.2 Popup（携帯端末）

360px のコンパクト管制端末。ワードマーク＋ステータスチップ → 巨大カウントダウン → イベント名＋数量チップ → 主アクション「設定を開く」。未設定時は空状態。

---

## 10. アクセシビリティ

- **コントラスト:** 全テキスト AA 以上（§2.4 実測済み）。
- **フォーカス:** `:focus-visible` に amber 環（`0 0 0 3px var(--te-signal-line)`）。キーボード操作を完全サポート。
- **色だけに頼らない:** 状態は色＋ラベル＋アイコン/形を必ず併用（amber/緑/赤 を識別できなくても判別可能）。
- **ヒット目標:** インタラクティブ要素は最小 24–32px。数量ステッパーの ± は大きめに。
- **モーション:** `prefers-reduced-motion` を尊重（§6）。
- **セマンティクス:** ランドマーク（`header`/`main`/`section`）、ラベルと入力の関連付け、アイコンボタンに `aria-label`、装飾アイコンに `aria-hidden`。

---

## 11. ライティング（マイクロコピー）

管制官のように **簡潔・操作的**。

- 状態は短く: `実行待機中 · T-00:11:42` / `券種を 3 件取得` / `確保成功`。
- 感嘆符を使わない。「〜してください」は最小限（誘導が必要な箇所のみ）。
- エラーは「何が起きたか → どうするか」を 1 文で。例外文字列を生で出さない。
- 例:
  - 成功: `確保成功` / `券種を 3 件取得しました`
  - 待機: `実行待機中。0 秒で購入 URL へ遷移します`
  - エラー: `URL が不正です。https://escape.id/… を指定してください`
  - 超過: `実行時刻を過ぎています。時刻を再設定してください`

---

## 12. 実装方針とトークン配布

- ビルド工程なし（vanilla 前提）を維持。
- **共有 CSS を切り出す:** 現在 `options.html` と `popup.html` に巨大なデザインシステム CSS が重複コピーされている。これを解消するため:
  - `styles/tokens.css` — §2 のカスタムプロパティ（ダーク正準＋ライト副系）。
  - `styles/components.css` — §8 のコンポーネント実装。
  - 両 HTML から `<link>` で読み込み、重複を一掃。
- 既存 `lib/shared.js` には触れず、見た目は CSS 層に集約する。
- すべての色・角丸・余白・モーションはトークン経由で参照（生 hex を散らさない）。
- 既存の状態クラス（`.attention` `.attention-standby` `col-standby` 等）は §3 の状態機械に合わせて命名・配色を更新。

---

## 13. 旧 → 新 移行マップ

| 旧 | 新 |
|----|----|
| アクセント `#6c63ff`（インディゴ紫） | `--te-signal #FFB224`（シグナルアンバー） |
| 紫グラデのロゴ角丸＋🎫 | レティクル＋T-0 ティックの 1 色マーク（§7.2） |
| 絵文字 `⚙️🎟📊🔗⏱🎪` | 単線 SVG スプライト（§7.1） |
| `body` のラジアルグラデ背景 | フラットな `--te-bg` 盤面 |
| ライト基調（`#edf0f7`） | **ダーク正準**（`#0B0E13`）＋ライト副系 |
| 大きく柔らかい影（`--sh1/2/3`） | 境界＋面の明度差。影はメニューのみ |
| 角丸 20px（`--r4`） | 14px（`--te-r-lg`、機械的に締める） |
| 青の standby（`--blue`） | amber の ARMED（状態機械 §3） |
| `.guide-steps`（静的チップ） | 光るステップレール（§8.7） |
| 素の数量 number 入力 | ±数量ステッパー（§8.8） |
| デザイン CSS の二重コピー | `styles/tokens.css` + `styles/components.css` 共有 |

---

*この仕様は「色＝計器の信号」を一貫させることで、抑制と精密さから生まれる "AI っぽくない" 質感を狙う。実装時は本ドキュメントを唯一の正典とし、逸脱が必要な場合はまず本書を更新する。*
